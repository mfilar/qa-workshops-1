// TODO: declare an object named person that has at least 3 key-value pairs

let person;

/**
 * Test
 * Do not modify lines below
 */
if (typeof person === 'object' && Object.keys(person).length >= 3) console.log("Yaay,  🎉");
