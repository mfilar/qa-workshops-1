const a = ["a", "b"];
// we can also declare an array with a call to global Array object
const b = new Array("a", "b");

// TODO#1: check the console for logs. will the arrays be the same?
console.log('#1', { a, b });

// ------------------------------------------------------------------
// TODO#2: declare an array "c" and "d" with 3 or more numbers using 2 ways

let c;
let d;

console.log('#2', { c, d });

// ------------------------------------------------------------------
// TODO#3, : declare an array "e" and "f" that include only one element - number 5. Use 2 ways.

let e;
let f;

// check what get's log in the console? Is it what you expected?
console.log('#3', { e, f });
